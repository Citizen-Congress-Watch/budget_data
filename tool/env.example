# Keystone GraphQL base URL，部署在雲端的 HTTPS endpoint
KEYSTONE_URL=<YOUR KEYSTONE_URL>

# 單次抓取資料量，預設 1000
# BATCH_SIZE=1000

# 每次示範輸出筆數，預設 10，正式匯出前可調高或拿掉
# MAX_RECORDS=10

# 若需要自訂輸出位置，可設定成絕對路徑，預設為 repo 根目錄
# OUTPUT_DIR=/absolute/path/to/repo

# 進階：若要覆寫 where 條件，可設定 JSON，例如只抓 2025 年
# PROPOSAL_WHERE_JSON={"publishStatus":{"equals":"published"},"year":{"id":{"equals":"year_2025"}}}
